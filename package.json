{
  "name": "banco-web-tests",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "cypress run --env URL=http://localhost:4000",
    "test-qa": "cypress run --env URL=http://qa-env:4000",
    "test-prod": "cypress run --env URL=http://localhost:3000",
    "cy:open": "cypress open",
    "cy:headed": "cypress run --headed"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "cypress": "^14.5.1"
  }
}
